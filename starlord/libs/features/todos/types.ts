// TODO: Use as template for the database layer when needed
// Or remove entirely and use the template in leui of this

export type TTodoItem = {
  id: string;
  title: string;
  description?: string;
  completed?: boolean;
};
